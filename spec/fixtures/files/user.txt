{"data" => {
   "id" => "#{user.id}",
   "type"=> "users",
   "attributes" => {
     "languages" => [],
     "location" => nil,
     "name" => "#{user.name}",
     "email" => "#{user.email}",
     "age" => user.age,
     "gender" => "#{user.gender}"
}}}
